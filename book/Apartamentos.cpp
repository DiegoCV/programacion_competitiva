#include <algorithm>
#include <iostream>
#include <vector>

using namespace std;

typedef vector<int> vi;

int main() {
  int n, x, i;
  cin >> n >> x;
  int pi[n];
  for (i = 0; i < n; i++) cin >> pi[i];
  sort(pi.begin(), pi.end());
  
}